import { DatabaseService, Room, Player, Property, Log, Auction, Trade } from '../database/database.service';
import { RoomsGateway } from './rooms.gateway';
import { AiService } from '../ai/ai.service';
export declare class RoomsService {
    private readonly db;
    private readonly gateway;
    private readonly ai;
    constructor(db: DatabaseService, gateway: RoomsGateway, ai: AiService);
    createRoom(cityCode: string, hostName: string, hostColor: string, hostPin?: string | null): Room;
    joinRoom(code: string, name: string, color: string, isBot?: boolean, botPersonality?: any, pin?: string | null): Player;
    startMatch(code: string): Room;
    rollDice(code: string, playerId: string): any;
    resolveLanding(code: string, playerId: string, slotId: number): any;
    buyProperty(code: string, playerId: string, slotId: number): Property;
    endTurn(code: string, playerId: string): Promise<Room>;
    takeLoan(code: string, playerId: string, amount: number): any;
    private recalculatePropertyValuations;
    private advanceEconomicCycle;
    private checkBankruptcy;
    private getActivePlayerIndex;
    addLog(roomId: string, message: string, color?: string | null): Log;
    startAuction(code: string, slotId: number): Auction;
    placeBid(code: string, auctionId: string, playerId: string, bidAmount: number): Auction;
    private scheduleAuctionEndCheck;
    private resolveAuction;
    proposeTrade(code: string, senderId: string, receiverId: string, offerCash: number, offerProperties: number[], requestProperties: number[]): Trade;
    resolveTrade(code: string, tradeId: string, resolution: 'ACCEPTED' | 'DECLINED'): Trade;
    triggerNewsNarrator(roomId: string, eventDescription: string): Promise<void>;
    runBotTurn(roomId: string, playerId: string): void;
    private getSlotType;
    private getCitySlotCost;
    private getCitySlotRent;
}
